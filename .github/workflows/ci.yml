name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ─────────────────────────────────────────────
  # Backend (FastAPI) — ruff lint + pytest
  # ─────────────────────────────────────────────
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: data_portal/src/api
    env:
      PYTHONPATH: ${{ github.workspace }}:${{ github.workspace }}/data_portal/src/api
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: data_portal/src/api/requirements.txt

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install "ruff==0.12.12" pytest pytest-asyncio pytest-cov pytest-timeout pytest-mock
          pip install \
            "fastapi==0.109.0" "uvicorn[standard]==0.27.0" "pydantic==2.5.3" \
            "pydantic-settings==2.1.0" "httpx==0.26.0" "python-multipart==0.0.6" \
            "redis==5.0.1" "SQLAlchemy==2.0.25" "openai==1.10.0" \
            "docker>=7.1.0" "psutil>=5.9.0" "prometheus-client>=0.20.0" \
            "aiosqlite>=0.19.0" "Faker>=38.0.0"
          # Heavy packages installed best-effort (pymilvus needs grpcio build)
          pip install "pymilvus>=2.4.0" "minio>=7.2.0" "asyncpg==0.29.0" \
            "langgraph>=0.2.0" "langchain-core>=0.2.0" || true

      - name: Ruff lint
        run: ruff check . --config ../../../ruff.toml

      - name: Pytest (unit only)
        run: |
          python -m pytest -m "not integration and not e2e" \
            --timeout=30 \
            --tb=short \
            -q \
            --ignore=tests/test_consent_mgmt.py \
            --ignore=tests/test_governance_ext.py

  # ─────────────────────────────────────────────
  # AI Services — ruff lint + pytest
  # ─────────────────────────────────────────────
  ai-services:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ai_services
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: data_portal/src/api/requirements.txt

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install "ruff==0.12.12" pytest pytest-asyncio pytest-timeout
          pip install \
            "fastapi==0.109.0" "pydantic==2.5.3" "httpx==0.26.0" \
            "openai==1.10.0" "SQLAlchemy==2.0.25" "aiosqlite>=0.19.0" \
            "langgraph>=0.2.0" "langchain-core>=0.2.0" || true

      - name: Ruff lint
        run: ruff check . --config ../ruff.toml

      - name: Pytest (unit only)
        run: |
          python -m pytest -m "not integration and not e2e" \
            --timeout=30 \
            --tb=short \
            -q \
            --ignore=rag/ \
            || true

  # ─────────────────────────────────────────────
  # Frontend (React/Vite) — eslint + vitest + build
  # ─────────────────────────────────────────────
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: data_portal/src/portal
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: data_portal/src/portal/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: ESLint
        run: npx eslint src --ext ts,tsx

      - name: Vitest
        run: npm test

      - name: TypeScript + Vite build
        run: npm run build
        env:
          NODE_OPTIONS: "--max-old-space-size=4096"
