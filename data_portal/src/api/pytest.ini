[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (requires DB)
    e2e: End-to-end smoke tests (requires all services)
filterwarnings =
    ignore::DeprecationWarning
timeout = 30
